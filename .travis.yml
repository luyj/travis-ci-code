sudo: required

language: sh

before_install:
    - sudo apt-get update -q
    - export scversion="stable" # or "v0.4.7", or "latest"
    - wget "https://storage.googleapis.com/shellcheck/shellcheck-${scversion}.linux.x86_64.tar.xz"
    - wget "https://media.githubusercontent.com/media/CUCCS/linux-2019-jckling/0x04/0x04/Codes/worldcupplayerinfo.tsv"
    - wget "https://media.githubusercontent.com/media/CUCCS/linux-2019-jckling/0x04/0x04/Codes/web_log.tsv"
    - wget "https://github.com/luyj/travis-ci-code/blob/Linux_exp0x04/Linux_exp0x04_code/test.jpg.jpg"
    - tar --xz -xvf shellcheck-"${scversion}".linux.x86_64.tar.xz
    - sudo cp shellcheck-"${scversion}"/shellcheck /usr/bin/
    - /usr/bin/shellcheck --version
    - sudo apt install 

script:
  - bash test.sh
  - bash ImageProcess.sh
  - bash WebLog.sh
  - bash WorldCupPlayerInfo.sh
  - bash ImageProcess.sh -h
  - bash WebLog.sh -h
  - bash WorldCupPlayerInfo.sh -h
  - bash ImageProcess.sh -cq 20
  - bash ImageProcess.sh -cs 50
  - bash WorldCupPlayerInfo.sh -sh
  - bash WorldCupPlayerInfo.sh -si
  - bash WorldCupPlayerInfo.sh -sc
  - bash WorldCupPlayerInfo.sh -u
  - bash WorldCupPlayerInfo.sh -uh "uplherc.upl.com"
  - bash WorldCupPlayerInfo.sh -fs
after_script:
  - /usr/bin/shellcheck test.sh

after_failure:
  - id
  - pwd
  - df -h
  - cat error.log

notifications:
  email:
    on_success: never
